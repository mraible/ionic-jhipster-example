version: '2'
services:
    oauth-postgresql:
        image: postgres:9.6.5
        # volumes:
        #     - ~/volumes/jhipster/oauth/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=oauth
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
