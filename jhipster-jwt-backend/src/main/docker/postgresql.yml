version: '2'
services:
    jot-postgresql:
        image: postgres:9.6.5
        # volumes:
        #     - ~/volumes/jhipster/jot/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=jot
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
